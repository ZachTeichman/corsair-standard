# Legacy Vanilla UI

This folder archives the pre-React vanilla UI:

- `index.html`
- `audit.html`
- `app.js`
- `styles.css`

The active product UI now lives in `frontend/src` and is served locally at `/app` through `server.py`. Keep these files only as historical reference unless explicitly reviving the old implementation.
